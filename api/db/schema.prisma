datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

generator client {
  provider      = "prisma-client-js"
  binaryTargets = "native"
}

model Booking {
  id       Int    @id @default(autoincrement())
  email    String @unique
  name     String
  lastName String
}
