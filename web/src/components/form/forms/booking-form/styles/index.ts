const dateWrap = {
  cursor: 'pointer',
  flexDirection: 'column',
  width: 'full',
}

const SubmitGhost = {
  border: 'none',
  background: 'none',
  padding: 0,
}

const alignSubmit = {
  justifyContent: 'flex-end',
  mt: 4,
}

const columGap = {
  columnGap: '20px',
}

export { dateWrap, SubmitGhost, alignSubmit, columGap }
