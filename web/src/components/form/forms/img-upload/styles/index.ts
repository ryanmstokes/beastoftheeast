const wrap = {
  position: 'fixed',
  top: 0,
  left: 0,
  right: 0,
  bottom: 0,
  display: 'flex',
  alignItems: 'center',
}

const modal = {
  display: 'block',
  margin: '0 auto',
  maxWidth: '520px',
  position: 'relative',
}

const btn = {
  position: 'absolute',
  right: 0,
  marginTop: '10px',
  marginRight: '5px',
  cursor: 'pointer',
}

export { wrap, modal, btn }
